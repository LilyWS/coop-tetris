import React from 'react';
import { Link } from 'react-router-dom';

const Header = () => {
  return (
    <header>
        <h2>CPTetris</h2>
        <nav>
            About | Leaderboard
        </nav>
    </header>
  );
};

export default Header;