import React from 'react';
import Header from '../components/Header';

const Home = () => {
  return (
    <main>
        <h1>CPTetris</h1>
        <button>Create Match</button>
    </main>
  );
};

export default Home;