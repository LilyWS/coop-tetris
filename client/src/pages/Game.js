import React from 'react';
import Canvas from '../components/Canvas';

const Game = () => {
  return (
    <main>
        <canvas id="game-display">
    
        </canvas>
    </main>
  );
};

export default Game;